# 2026-05-21 工作记录

## 按德勤风格重新设计存勤法税官网

### 背景
- 用户要求参考德勤中国官网（https://www.deloitte.com/cn/zh.html）重新设计存勤法税官网
- 德勤官网特点：专业稳重、深蓝配色、大量留白、卡片式布局、清晰的视觉层次

### 遇到的问题
1. **Hexo `source/index.html` 生成问题**：
   - 第一次尝试：写了 YAML front-matter（`layout: false`），Hexo 把文件当作文章处理，用 Fluid 主题渲染，导致生成的 `public/index.html` 仍然是 Fluid 主题的文章列表页面
   - 第二次尝试：去掉 front-matter，直接写纯 HTML，Hexo 正确原样复制到 `public/index.html`
   - **结论**：Hexo 对 `source/` 下的 HTML 文件，有 front-matter 就渲染，没有就原样复制

2. **部署后访问还是旧版本**：
   - 原因：CDN/浏览器缓存
   - 解决：清除浏览器缓存（Ctrl+Shift+R）后刷新

### 最终方案
- `source/index.html`：纯 HTML 文件，无 front-matter，包含完整的德勤风格页面（导航栏、Hero 首屏、数据条、服务卡片、关于区块、客户案例、文章列表、联系我们、页脚）
- `source/css/custom.css`：德勤风格自定义 CSS（配色、布局、响应式）
- 配色方案：
  - 主色：深蓝 `#003f6c`（德勤蓝）
  - 点缀色：青绿 `#00a19c`
  - 辅助色：浅灰 `#f8f9fa`、深灰 `#333`、`#666`

### 部署记录
- Commit: `f659862` - "fix: 修复首页生成问题，使用纯HTML独立页面（德勤风格）"
- 已推送到 GitHub，Actions 正在自动部署

### 待确认
- 用户访问 https://Deng-dahua.github.io/cunqin_law-tax/ 查看效果
- 是否需要调整颜色、布局、内容等

---

## 联系我们模块修正

### 修改内容
1. **删除"电子邮箱"**：移除联系信息中的邮箱条目（`contact@cunqin.cn`）
2. **更新微信二维码**：用用户提供的最新微信二维码图片替换 `/images/wechat-qrcode.jpg`

### 部署记录
- Commit: `dee6cd6` - "fix(contact): 删除电子邮箱；更新微信二维码图片"

---

## 全面整改（用户要求认真仔细检查官网）

### 发现的问题
1. **页脚仍显示"邮箱contact@cunqin.cn"** —— 之前只删了联系模块，没删页脚
2. **所有图片/链接路径用了绝对路径** `/images/...`、`/about/` 等 —— 在GitHub Pages项目站点会指向根域名而非 `/cunqin_law-tax/...`
3. **缺少 `.nojekyll` 文件** —— GitHub Pages会用Jekyll处理，可能引发路径问题
4. **`public/index.html` 始终显示Fluid主题版本** —— Hexo的 `index_generator` 会强制生成文章列表页覆盖自定义首页

### 修复内容
1. **删除页脚邮箱**：移除页脚联系信息中的 `邮箱：contact@cunqin.cn`
2. **批量修复路径**：所有 `/images/...` → `images/...`，所有 `/about/` → `about/` 等（改为相对路径）
3. **创建 `.nojekyll`**：防止GitHub Pages用Jekyll处理
4. **`_config.yml` 添加 `skip_render: index.html`**：防止Hexo渲染 `source/index.html`
5. **`.gitignore` 添加 `.workbuddy/`**：避免工作内存文件误入仓库
6. **替换创始人照片**：`founder.jpg` 更新为用户提供的"总经理形象照.png"
7. **替换微信二维码**：`wechat-qrcode.jpg` 更新为最新版本

### 部署记录
- Commit: `179d346` - "fix: 全面修复官网（页脚邮箱/图片路径/skip_render/.nojekyll）"
